export type Product = { id: string; name: string; supplier: string; price: number; tag: string; image: string; stock: number };
export type PayMethod = "cash" | "card" | "qris";
export type GrindOption = "whole" | "ground";